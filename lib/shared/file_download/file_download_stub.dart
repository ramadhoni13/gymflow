Future<void> downloadReportFile({
  required List<int> bytes,
  required String filename,
  required String mimeType,
}) async {
  throw UnsupportedError('Download file tidak didukung di platform ini.');
}
